# CRA source maps are not used by a production error service and their output
# is nondeterministic across otherwise identical builds. Keep deploy artifacts
# cache-stable and avoid publishing the full source tree.
GENERATE_SOURCEMAP=false
